var encontrado = colores.find(function(elemento){
    return elemento === 'rojo';
});
var encontrado = colores.findIndex(function(elemento){
    return elemento === 1;
});
function saludar (nombre){
    return "hola, " + nombre + "!"
}
const mensaje = saludar (" bismarck")